{
  "name": "workstation-checkup-proxy",
  "version": "1.0.0",
  "description": "Internal API proxy for Workstation Checkup — verifies Firebase ID tokens, forwards to Toqan + Jira with server-held (Vault) secrets. Runs internal-only on Sonic Runtime (Option C).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.2.0",
    "firebase-admin": "^13.0.0"
  }
}
