# Local dev only. On Sonic Runtime these come from Vault via the sidecar (see README helmfile snippet).
# NEVER commit real values — this file is a template.
TOQAN_API_KEY=
JIRA_BASE_URL=https://justeattakeaway.atlassian.net
JIRA_EMAIL=                 # Jira service-account email (for Basic auth) — CONFIRM this is the scheme
JIRA_TOKEN=                 # Jira service-account API token
ALLOWED_ORIGIN=https://workstation-revamp.web.app
ALLOWED_DOMAIN=@justeattakeaway.com
FIREBASE_PROJECT_ID=workstation-revamp
PORT=8080
