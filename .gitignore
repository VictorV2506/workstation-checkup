# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log

# Environment — never commit secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node (not used now, but safe to include)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude local settings (machine-specific; skills/ + launch.json stay shared)
.claude/settings.local.json

# report-portal — dead/superseded hand-rolled proxy, kept local only
report-portal/
